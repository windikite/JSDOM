// Assignment 3
// Task2
function change_color(event, color) {
     event.style.color = color;
}

function change_background(event, color) {
     event.style.backgroundColor = color;
}

function change_border(event, color) {
     event.style.borderColor = color;
}

function move(event, distance) {
     event.style.marginLeft = "250px";
}
